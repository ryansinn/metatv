# MetaTV — Code Audit (2026-06-19)

Full-codebase audit run on clean `main` (@ #62) after the player-reliability /
Split-Streams / unified-menu / EPG-relink / What's-New feature arc and the Band-9 cosmetic
cleanup. Audited against the **codified Critical Rules** in `CLAUDE.md` (incl. the three
chokepoint rules added in #62). Baseline: **1002 tests green**.

Method: (1) an objective rule-violation sweep (grep/`wc` over `metatv/`), then (2) a judgment
pass on the results. This doc records the findings and the **Band 10** remediation plan.

---

## ✅ Healthy — what held up

- **Color tokens: 100%.** Zero raw hex / rgba literals in widget code. The two-layer `theme.py`
  color discipline is fully intact.
- **Unified channel-menu registry holds.** No new hand-rolled channel-action menus. The 12
  `QMenu(` sites are all legitimately scoped out: filter dropdowns (`filter_bar`,
  `sports_filter_bar`, `filter_panel`), column-header menu (`customizable_tree`), details chips
  (`details_versions`, `details_similar`), the series menu (`main_window.py:2635`), the
  Preferences variant-dedup pre-menu (`sidebar/recommended.py:176`), the queue empty-space
  "Clear Unavailable" fallback (`sidebar/queue.py`), and the composer itself (`channel_menu.py`).
- **`provider_id` threaded** through both play paths (`main_window_streaming.py:406`,
  `main_window_nav.py:155`) — Split Streams keying intact.
- **Async-read seams** (`_run_query`, `BackgroundRefreshMixin`) in place; **EPG `channel_name`**
  populated at fetch (relink intact). **1002 tests green.**

---

## 🔧 Findings (prioritized)

### P1 — `session.expunge` → DTO  *(Band 9 B9-2, still open)*
**12 sites** in `main_window_metadata.py` + `main_window_favorites.py` (grew from 9 — the new
`play_channel_new_window_by_id` added one). The CLAUDE.md "ORM must not outlive its session"
rule names `expunge` the *fragile fallback*, safe only while the ORM stays relationship-free.
Build a `PlayableDTO` (+ `PlayableEpisodeDTO`) inside the `session_scope` and drop the expunge.
Detailed plan: `docs/REFACTOR_PLAN_BAND9.md` § B9-2.

### P1 — render-time `parse_channel_name` drift
**9 GUI sites; only 1 is the documented exception** (`epg_view._make_recommendation_item`). The
rest violate the ingestion-only rule (read stored `detected_*`, don't re-parse at render):
- `gui/details_sections.py:360` (`load_basic`), `:460` (`load_metadata` — parses *metadata* title; borderline)
- `gui/details_similar.py:116`
- `gui/sidebar/base.py:23`
- `gui/main_window.py:2568`
- `gui/epg_view.py:1639`, `:1792` (watchlist / My-Channels render)

The EPG On-Now fix (#53) addressed only one spot; this is the same stored-field pattern applied
to the rest. Each needs the consuming surface to read `detected_prefix`/`detected_title`/etc., or
a DTO carrying them (some, like `details_similar`, already have a DTO that could carry the fields).

### P2 — files over 1000 lines
`epg_view.py` **2959**, `main_window.py` **2910** (both *grew* during the feature push),
`provider_editor.py` **1291**, `channel.py` **1016**. The long-deferred decomposition
(B7-7/8). Candidate splits: EPG tab modules (Watchlist/OnNow/Browse/Events/Discover); a
MainWindow provider-toggle / channel-load orchestration mixin; `channel.py` query split.

### P2 / rules-alignment — the `px` half of the theme rule is not enforced
0 hex, but **133 `font-size: Npx`** literals bypass the existing `FONT_*` tokens, plus **61
structural `padding`/`border-radius` px** that have *no* token. The rule as written ("no px
literal") does not match reality ("colors always tokenized; sizes often inline"). **Resolve the
rule first** (see Band 10 item B10-3), then a gradual `font-size`→`FONT_*` cleanup. Worst files:
`epg_view.py` (16), `similar_lightbox.py` (15), `global_filter_dialog.py` (11),
`categories_dialog.py` (10).

### P3 — long tail (low urgency)
- **`get_session()` → `session_scope`:** 76 sites / 23 files. Rule already permits legacy; migrate
  opportunistically. (New EPG-relink code uses `get_session()` to match the surrounding
  `EpgManager` pattern — acceptable, noted.)
- **Band 9 B9-1** (`load_channels` → `_run_query` seam) — still open; needs a display smoke-test.
- **`executor.submit` outside the seam:** ~22 sites — mostly legit (seam internals, EPG/image
  workers); a per-site pass could fold a few into `_run_query`. Low value.

---

## Band 10 — remediation plan

One concern per PR; behavior-preserving; pin each with a test that executes the changed path.
B10-1/B10-2 are P1.

| ID | Task | Risk / verify |
|---|---|---|
| **B10-1** | ✅ DONE (#65) — `session.expunge` → `PlayableChannelDTO`/`PlayableEpisodeDTO` across the 10 sites. | — |
| **B10-2** | ✅ DONE (#67) — render-time `parse_channel_name` removed from `load_basic` + EPG watchlist rows (stored fields). Deferred: `details_similar`, `sidebar/base`, notification, `load_metadata`. | — |
| **B10-3** | ✅ DONE (#68 rule + #69) — rule amended (CLAUDE.md + UI_UX_GUIDELINES): colors hex/rgba strict; `font-size` → `FONT_*`; structural spacing px acceptable inline. The 133 inline `font-size` literals migrated to `FONT_*` tokens. | — |
| **B10-4** | ✅ `epg_view.py` DONE (#70/#71/#72 + #73): 2191 → 721 lines via `_EpgEvents/Browse/OnNow/Watchlist` mixins + shared `epg_widgets.py`. **Remaining:** `main_window.py` decomposition — see the concern-map pass below (judged by isolation, not the 1000-line number). | Behavior-preserving verbatim-mixin moves; full suite as the guard. |
| **B10-5** | ✅ DONE (#74) — `load_channels` → `_run_query` seam; channel list crosses the boundary as `ChannelListDTO` (no ORM across the thread boundary anywhere now). ORM rule re-articulated around purpose. | Needs `./run.sh` display smoke-test before merge. |

Deferred / opportunistic: `get_session()`→`session_scope` migration (P3), `executor.submit`
seam folds (P3).

---

## `main_window.py` concern map — split by isolation, not the line count

**Premise (per the user's 2026-06-19 reflection — see `feedback_self_documenting_modularity`):**
the goal is **surgical change + comprehension**, not "< 1000 lines." Judge every proposed split by
*"can a dev (or an AI) find and change one concern without wading through unrelated ones, and know
from the filename where it lives?"* The 1000 number is only a *smell* that triggers this look.
`main_window.py` is 2921 lines and already delegates 5 mixins (`_Streaming`, `_Nav`, `_Metadata`,
`_Favorites`, `_Async`). What remains is **six concerns tangled in one file** — three of them large
and cleanly isolatable. The extraction uses the proven verbatim-mixin pattern (behavior-identical
moves, shared `self`/MRO, full suite as the guard — exactly how `epg_view.py` went 2191 → 721).

**The remaining concerns (every non-shell method, bucketed):**

| Concern | Methods (representative) | Lines | Verdict |
|---|---|---|---|
| **A — Window shell (the irreducible core)** | `__init__`, `setup_ui`, `create_menu_bar`/`_sidebar`/`_section`/`_bottom_nav_bar`, `create_content_area` (235 l), `setup_notifications`, `resizeEvent`, `_apply_sidebar_visibility`, `_register_cleanable`, `closeEvent`, persistence (`save_splitter_sizes`, …) | ~900 | **Stays.** This *is* "MainWindow is a QMainWindow": assemble the panels, own lifecycle/cleanup. One coherent concern — leave it. |
| **B — Provider/source lifecycle** | `add_provider`, `enter/exit_provider_edit/analytics_mode`, `toggle_provider_active`/`_visibility`, all `_on_provider_*` handlers, `_refresh_provider_dependent_views` (the chokepoint), `refresh_provider`, `on_provider_refresh_finished`, `refresh_all_providers`, `on_provider_selected*`, `load_providers`, `test_*_provider*`, `update_provider_status` | ~430 | **Extract → `_ProviderMixin` (B10-6).** Biggest, most cohesive, highest-isolation cluster. Self-contained domain (add/edit/delete/refresh/toggle/test + the canonical refresh chokepoint). Touches only `self.*` + importable `AddProviderDialog`/`ProviderLoadThread`/`RepositoryFactory`. |
| **C — Series / episode drill-down + playback** | `drill_into_series`, `on_series_loaded`, `populate_series_tree`, `on_tree_item_expanded`/`collapsed`, `play_series_item`, `play_episode`, `launch_player_for_episode`, `_on_episode_stream_unavailable`, `_do_launch_episode`, `show_series_context_menu`, `toggle_episode_watched`, `play_channel` | ~400 | **Extract → `_SeriesMixin` (B10-7).** Cohesive drill-down/episode tree domain, distinct from live-channel streaming (`_StreamingMixin`). |
| **D — Channel list load + filter pipeline** | `load_channels`, `_query_channels`, `_on_channels_loaded`/`_load_error`, `filter_channels`, `on_filter_changed`, `initialize_filter_stats`, `_on_filter_stats_loaded`, `_open_category_picker`, `toggle_filters`, `_on_search_text_changed` | ~360 | **Extract → `_ChannelListMixin` (B10-8).** The list/search/filter pipeline. *Sequence after B10-5 merges* — B10-5 rewrote these exact methods. |
| **E — Context-menu glue** | `_show_channel_menu`, `_bg_fetch_ctx_data`, `_on_ctx_data_ready`, `_build_handlers`, `_show_context_menu_for` | ~190 | **Fold into D (B10-8).** Small; it's the channel-list's action seam — same concern. |
| **F — Sidebar watch-alerts glue** | `refresh_sidebar`, `_refresh_watch_alerts`, `_watch/_unwatch_channel_from_list`, `_prompt_track_from_list` | ~35 | **Stays** (thin shell glue) or fold into a future sidebar mixin. Too small to extract alone. |
| **G — Modal/dialog entry-exit + What's New** | `on_diagnose_clicked`, `on_split_toggle_clicked`, `_show_similar_lightbox`, `show_operations`/`_diagnostics`/`_about`, `open_settings`, `manage_filters`, `_whats_new_unseen`, `show_whats_new`, `maybe_show_whats_new` | ~120 | **Stays** for now (small, shell-adjacent). Revisit if it grows. |

**Extraction sequence (one PR each, off fresh `main`, suite-guarded).** Each PR edits the class
declaration (line 172) + the mixin-import block, so they **conflict if stacked unmerged** — do them
strictly sequentially, merge before the next:

1. **B10-6 — `_ProviderMixin`** → `main_window_providers.py`. Conflict-free with the unmerged B10-5
   (different methods); can start now.
2. **B10-7 — `_SeriesMixin`** → `main_window_series.py`. After B10-6 merges.
3. **B10-8 — `_ChannelListMixin`** (concerns D + E) → `main_window_channels.py`. **After B10-5 merges**
   (it rewrote these methods) and after B10-7.

Net: `main_window.py` drops to ~900 lines that are *one* concern (assemble + own the window), and
each extracted file is named for exactly what lives there — the file tree itself tells you where to
look. The number falls out of the isolation; it was never the target.
