# Core dependencies
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0
qtawesome>=1.3.0

# Database
SQLAlchemy>=2.0.0

# HTTP & Async
aiohttp>=3.9.0
requests>=2.31.0

# Data processing
pydantic>=2.5.0
python-dateutil>=2.8.2

# Logging
loguru>=0.7.0

# Configuration
PyYAML>=6.0.1

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-qt>=4.2.0
# Lint. Configured in pyproject.toml; enforced by the `lint` job in CI.
# It replaces black and mypy, which were declared here for a year with no
# config, no CI step and no script that ran them. alembic went the same
# way: declared, never imported, no migrations directory.
ruff>=0.6.0
