{
  "_comment": "Files exempt from tests/test_no_stray_glyph_literals.py, keyed by repo-relative path with a one-line reason. SHRINK-ONLY: the guard's _ALLOWLIST_CEILING is the number of entries allowed, and it is 0 — adding one means raising that constant in the test, which is visible in review. Aim to keep this empty; a glyph belongs in metatv/gui/icons.py."
}
